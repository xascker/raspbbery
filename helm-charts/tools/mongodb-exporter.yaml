mongodb:
  uri: "mongodb://user:password@mongodb-primary:27017/admin"

resources:
  requests:
    memory: 64Mi
    cpu: 25m
  limits:
    memory: 128Mi
    cpu: 100m

serviceMonitor:
  enabled: true
  namespace: default
  labels:
    release: prometheus
