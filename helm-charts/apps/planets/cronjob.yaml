apiVersion: batch/v1
kind: CronJob
metadata:
  name: planets-collector
  namespace: astro

spec:
  schedule: "5 1 * * *"   # 06:00 MST
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 3

  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never

          containers:
            - name: planets-collector
              image: xascker/planets-collector:0.11
              imagePullPolicy: IfNotPresent

              env:
                - name: APP_CONFIG
                  valueFrom:
                    secretKeyRef:
                      name: planets-properties
                      key: APP_CONFIG

              resources:
                requests:
                  cpu: "20m"
                  memory: "64Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"