apiVersion: batch/v1
kind: CronJob
metadata:
  name: sun-collector
  namespace: astro

spec:
  schedule: "0 1 * * *"   # 01:00 UTC
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 3

  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never

          containers:
            - name: sun-collector
              image: xascker/sun-collector:0.1
              imagePullPolicy: IfNotPresent

              env:
                - name: MONGO_URL
                  valueFrom:
                    secretKeyRef:
                      name: mongo-secret
                      key: MONGO_URL

              resources:
                requests:
                  cpu: "20m"
                  memory: "64Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"